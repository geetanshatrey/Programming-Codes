// Time Complexity : O(N)
// Problem : Maximum Subarray Sum Using Kadane's Algorithm
#include<iostream>
#include<climits>
#include<map>
#include<vector>

using namespace std;

void pairSum(int arr[], int n) {
    vector<int> res;
    map<int, int> vals;
    for(int i = 0; i < n; i++) {
        if(vals)
        vals.insert(make_pair())
    }
}

int main() {
    int arr[8] = {1,3,5,8,8,11,12,13};
    int n = 8;
    pairSum(arr, n);
}
